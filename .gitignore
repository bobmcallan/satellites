# Claude Code per-machine settings (contains satellites hook token)
.claude/settings.local.json

# Local .env (use .env.example as the committed template)
.env

# Local TOML config (use satellites.example.toml as the committed template)
satellites.toml

# Go build output
/satellites
/satellites-agent
*.exe
*.test
*.out
*.prof

# Dependencies
/vendor/

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store
