# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/
.cursor/mcp.json

# Build output
/bin/
/dist/

# Satellites — .satellites/ is the client's config/state home (ignored). The
# DEFAULT authoring locations .satellites/{documents,principles,skills}/ are the
# tracked exceptions; a repo that RELOCATES a kind via [substrate_roots] (e.g. a
# skills repo with skills = ".") tracks it at the configured path instead.
# Workflows are repo-owned config (epic:client-dir-separation order-2), tracked
# at .satellites/workflows/ — the governing-workflow resolver's client-dir home.
.satellites/*
!.satellites/documents/
!.satellites/principles/
!.satellites/skills/
!.satellites/workflows/
!.satellites/seeds/
# This repo's own client config is committed (non-secret: the api-key lives in
# the user credential store, never the toml) so its consumption — notably the
# library_pins (workflows unpinned per order-2) — is shared, not per-clone.
!.satellites/satellites.toml

/scripts/satellites-server.toml
/satellites.toml

# Operator-local backup of .claude/skills (created during a manual move). Never committed.
.claude_bak/

# Windows Crap
*:Zone.Identifier

# Skill-sync conflict backups (`satellites skill sync --on-conflict force`).
# Local safety copies of an overwritten edit — never committed.
.claude/skills/**/*.bak
