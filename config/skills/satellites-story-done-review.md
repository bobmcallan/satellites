---
name: satellites-story-done-review
type: skill
kind: reviewer
when: status==in_progress
tags: [kind:reviewer]
description: The default STORY done gate — judges that an engaged story has actually reached its terminal goal (its numbered acceptance criteria are satisfied with evidence in the body/ledger) before it closes. The exit gate of the default/baseline workflow, the sibling of the entry gate satellites-intent-plan-review. It also requires two default datapoints — the story's ACTUAL token usage (an `actual-tokens` tag self-reported via `satellites story actual`) and an attached PR-like CHANGE DOCUMENT — a story-linked `type:summary` doc carrying the narrative of what changed and why, the git record, and the ACTUAL WORKFLOW as YAML + mermaid (generated by `satellites story changedoc`). The per-stage narrative is owned by the renamed `satellites-story-stage-summary` reviewer; this gate owns the done-stage change document. A missing actual or a missing/inadequate change document is REJECTED (presence + diff coverage judged, not prose quality). Emits {decision, notes} JSON.
---

Decide ONE thing: is this story **actually done** — has the work reached the
story's own goal, with its numbered acceptance criteria satisfied? This is the
baseline exit gate every repo gets; it stops a story closing on "looks finished"
rather than evidence. It does NOT judge code quality, config-over-code, or the
commit/push (a richer repo-owned workflow composes those gates).

## Input

One JSON object on stdin carrying `story_id`, `project_id`, `workspace_id`,
`story_status` (current state) and `story_body` (markdown containing the numbered
acceptance criteria and a `## Workflow` fenced yaml block). No `next_status` —
the client resolves the target from that workflow on your accept. The gate READS
this context and JUDGES only — it writes nothing; the client enacts your verdict.

## Decision rule

Judge the `story_body` (and any evidence it cites — a summary, a ledger of
work done):

- **accept** — every numbered acceptance criterion is satisfied, with the body
  showing HOW (the change made, the check that passed), the story's goal is
  reached, the story records its ACTUAL token usage (an `actual-tokens` tag,
  self-reported via `satellites story actual`; see *Actual*), AND a PR-like
  CHANGE DOCUMENT is attached (a story-linked `type:summary` doc that names what
  changed and why, covers the real diff, and carries the actual-workflow YAML +
  mermaid; see *Change document*). The story is genuinely at a terminal `done`,
  not merely "code written" or "tests pass locally" asserted without trace.
- **reject** — one or more acceptance criteria are unmet, unevidenced, or
  contradicted; the body shows the work is partial; the goal is not yet reached;
  **the story records no actual token usage** (no `actual-tokens` tag — see
  *Actual*); **or no adequate change document is attached** (none present, or it
  does not cover the diff — see *Change document*). Name exactly what is missing.

Fail closed: if the story body cannot be read or its completion cannot be
judged, reject with the reason named.

## Actual

A closed story records what it actually cost, so the close-out panel can show
actual against the plan estimate. Elapsed time and reject count are derived
automatically, but there is no automatic token feed — the executor self-reports
its token usage. Judge PRESENCE only (not accuracy):

- Read the story's tags: `satellites story get <story_id>`.
- The story must carry an `actual-tokens` tag. If it does → the actual is
  recorded.
- If it is absent → **reject**. Tell the executor to record it with
  `satellites story actual <story_id> --tokens <n>` and re-request this gate.
  You JUDGE presence only — you do NOT set the tag.

## Change document

A closed story leaves one readable, PR-shaped record of its change — the third
default datapoint (its done-stage narrative complements the per-stage
`satellites-story-stage-summary`). The executor PRODUCES it with
`satellites story changedoc <story_id> [--summary <md>]`, which attaches a
story-linked `type:summary` document carrying: a short narrative (what changed
and why), the GIT RECORD (commits referencing the story + files changed), and the
ACTUAL WORKFLOW as both a YAML projection and a mermaid flowchart. You JUDGE it —
you do not write it:

- List the story's change documents (read-only):
  `satellites document list --scope project --tags story:<story_id>,type:summary`.
- A substantive attached `type:summary` document must exist. An inline body
  section does NOT satisfy — it must be an attached, story-linked document.
- It must COVER THE REAL DIFF: the files/packages it names are consistent with
  `git show --stat HEAD` (and the commits referencing the story). It must explain
  what changed and WHY, not merely list files, and must not claim work the diff
  does not show.
- If no such document exists, or it does not cover the diff → **reject**, telling
  the executor to generate it with `satellites story changedoc <story_id>` and
  re-request. Judge presence + diff coverage, not prose polish.

## Environment

You are a reviewer. You read the story body and judge it; you write NOTHING — no
ledger rows, no `document_upsert`, no git/file mutation. The client enacts your verdict.

```yaml
guardrails:
  always:
    - Judge whether the story's numbered acceptance criteria are satisfied with evidence and the goal is reached.
    - Require the story's ACTUAL token usage (an `actual-tokens` tag — a MISSING actual is a reject; judge presence, not accuracy).
    - Require an attached PR-like CHANGE DOCUMENT (a story-linked `type:summary` doc that covers the real diff and carries the actual-workflow YAML + mermaid; generated by `satellites story changedoc`) — a missing or diff-inconsistent change document is a reject. You may run read-only `satellites document list` + `git show --stat HEAD` to verify coverage; judge presence + coverage, not prose polish.
    - Verify the story's ## Workflow declares a transition whose from == story_status AND reviewer_skill == satellites-story-done-review — if none exists, reject; the client enacts that edge on your accept.
    - Fail closed — if the story body or its completion cannot be judged, print reject with the reason named.
    - The gate writes nothing — emit exactly one JSON object {decision, notes} as its only output.
  ask_first: []
  never:
    - Judge code quality, config-over-code, or the commit/push — richer repo-owned gates own those.
    - Accept when the workflow declares no matching transition from this status for this gate.
    - Write anything at all — no ledger_append, no document_upsert, no other mutating exec. The gate is judge-only; the client enacts.
```

## Output

Print exactly one JSON object and nothing else — no prose, no fence:

```json
{"decision": "accept", "notes": "one or two sentences; on reject, name exactly which acceptance criteria are unmet or unevidenced"}
```

`decision` is `accept` or `reject`. On reject, `notes` must name the specific
unmet criteria.
